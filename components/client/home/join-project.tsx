"use client";
import { Button } from "@/components/ui/button";

export default function JoinProjectBtn() {
  const joinProjectHandler = () => {};

  return (
    <Button variant="outline" size="lg" onClick={joinProjectHandler}>
      Join Project
    </Button>
  );
}
